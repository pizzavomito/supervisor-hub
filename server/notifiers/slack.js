

module.exports = class SlackNotifier {
  hook
  supervisor
  // eslint-disable-next-line no-useless-constructor
  constructor({webkook}) {
    // this.hook = new webhook.Webhook(webkook)
  }

  notifyError(msg) {

  }
}
